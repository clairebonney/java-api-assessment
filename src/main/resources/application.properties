#create (line3) will overwrite any new entry that is put into the databse.  Change the command to update when needed -especially during dev testing.  If you delete a customer, that id will not be reused (auto increment).  To start again from 1, use the create command instead of update, then start again -especially for testing purposes

spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:mysql://localhost:3306/Bakery
spring.datasource.username=demoUser12
spring.datasource.password=dem0dem0
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.open-in-view=true

